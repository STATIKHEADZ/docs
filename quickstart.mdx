---
title: 'Quickstart'
description: 'Create your first WURK job in under 5 minutes'
---

## Prerequisites

Before you begin, you'll need:

<CardGroup cols={2}>
  <Card title="API Key" icon="key">
    Get your API key from [wurk.fun/apikey](https://wurk.fun/apikey)
  </Card>
  <Card title="Payment Method" icon="wallet">
    Either USDC on Solana or platform balance (SOL)
  </Card>
</CardGroup>

## Create Your First Job

Let's create a simple social media repost job:

<CodeGroup>

```bash cURL
curl -X POST "https://wurkapi.fun/api/external/jobs/create" \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "social",
    "tweet_url": "https://x.com/yourhandle/status/1234567890",
    "min_rank": 1,
    "cooldown_minutes": 5,
    "jobtype": "repost",
    "max_completions": 100,
    "total_usdc": 5.0
  }'
```

```javascript Node.js
const response = await fetch('https://wurkapi.fun/api/external/jobs/create', {
  method: 'POST',
  headers: {
    'X-API-Key': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    type: 'social',
    tweet_url: 'https://x.com/yourhandle/status/1234567890',
    min_rank: 1,
    cooldown_minutes: 5,
    jobtype: 'repost',
    max_completions: 100,
    total_usdc: 5.0
  })
});

const data = await response.json();
console.log('Job created:', data.jobId);
```

```python Python
import requests

response = requests.post(
    'https://wurkapi.fun/api/external/jobs/create',
    headers={
        'X-API-Key': 'YOUR_API_KEY',
        'Content-Type': 'application/json'
    },
    json={
        'type': 'social',
        'tweet_url': 'https://x.com/yourhandle/status/1234567890',
        'min_rank': 1,
        'cooldown_minutes': 5,
        'jobtype': 'repost',
        'max_completions': 100,
        'total_usdc': 5.0
    }
)

data = response.json()
print(f"Job created: {data['jobId']}")
```

</CodeGroup>

## Response

```json
{
  "jobId": "abc123def",
  "status": "unpaid",
  "type": "social",
  "balancePayment": 0
}
```

## Complete the Payment

Since we used USDC payment, we need to complete the x402 payment:

<Steps>
  <Step title="Get Payment Requirements">
    Request the payment details for your job:
    
    ```bash
    curl -X GET "https://wurkapi.fun/api/x402/jobs/abc123def/pay" \
      -H "Accept: application/json"
    ```
    
    This returns a 402 status with payment requirements.
  </Step>
  
  <Step title="Send Payment">
    Use the x402 library to create and send the payment header:
    
    ```javascript
    import { createSigner } from 'x402/types';
    import { selectPaymentRequirements, createPaymentHeader } from 'x402/client';
    
    // Create signer with your Solana private key
    const signer = await createSigner('solana', YOUR_PRIVATE_KEY);
    
    // Select payment requirements
    const selected = selectPaymentRequirements(accepts, ['solana'], 'exact');
    
    // Create payment header
    const xPayment = await createPaymentHeader(signer, x402Version, selected);
    
    // Send payment
    const payResponse = await fetch('https://wurkapi.fun/api/x402/jobs/abc123def/pay', {
      headers: {
        'Accept': 'application/json',
        'X-PAYMENT': xPayment
      }
    });
    ```
  </Step>
  
  <Step title="Verify Success">
    A successful payment returns:
    
    ```json
    {
      "ok": true,
      "paid": true,
      "jobId": "abc123def"
    }
    ```
    
    Your job is now active!
  </Step>
</Steps>

## Alternative: Use Platform Balance

If you have SOL balance on the platform, you can skip the x402 payment:

```bash
curl -X POST "https://wurkapi.fun/api/external/jobs/create" \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "social",
    "tweet_url": "https://x.com/yourhandle/status/1234567890",
    "min_rank": 1,
    "cooldown_minutes": 5,
    "jobtype": "repost",
    "max_completions": 100,
    "balance_payment": 1,
    "total_bounty_sol": 0.12
  }'
```

Response for balance payment:
```json
{
  "jobId": "xyz789abc",
  "status": "pending",
  "type": "social",
  "balancePayment": 1
}
```

<Note>
  Jobs paid with platform balance activate immediately - no additional payment step needed!
</Note>

## What's Next?

<CardGroup cols={2}>
  <Card
    title="Explore Job Types"
    icon="briefcase"
    href="/concepts/job-types"
  >
    Learn about social vs custom jobs
</Card>
  <Card
    title="Manage Submissions"
    icon="list"
    href="/api-reference/submissions"
  >
    Track and manage job submissions
</Card>
  <Card
    title="Payment Details"
    icon="credit-card"
    href="/concepts/payment-methods"
  >
    Understand payment calculations
</Card>
  <Card
    title="Full API Reference"
    icon="book"
    href="/api-reference/introduction"
  >
    See all available endpoints
</Card>
</CardGroup>